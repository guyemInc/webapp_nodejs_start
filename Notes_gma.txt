Janvier 2026

Tutoriel : https://www.youtube.com/watch?v=U9TozNEs8q0

Installation nodejs
	+ installation des dépdances

	C:\Users\g.malinge>node -v
v25.2.1

Premier projet ;
	C:\Informatique\_tmp\Perso\Projets\webapp_nodejs_start

Passer de la console à un website
	https://www.youtube.com/watch?v=LVdw98jkfkk
	prerequis :
		VisualStudio
		nodejs
		npm

	npm -v
	11.6.2

Branché sur repository github

npm init

Press ^C at any time to quit.
package name: (webapp_nodejs_start)
version: (1.0.0)
description:
entry point: (index.js)
test command:
git repository: https://github.com/guyemInc/webapp_nodejs_start.git
keywords:
author:
license: (ISC)
type: (commonjs)
About to write to C:\Informatique\_tmp\Perso\Projets\webapp_nodejs_start\package.json:

{
  "name": "webapp_nodejs_start",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/guyemInc/webapp_nodejs_start.git"
  },
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/guyemInc/webapp_nodejs_start/issues"
  },
  "homepage": "https://github.com/guyemInc/webapp_nodejs_start#readme"
}

Cliquer sur ok pour la création du fichier

npm install express --save

> Cela installe les dépendances dans le dossier node_modules

Création du fichier index.js

Puis du fichier public/index.html

Depuis la console
	node index.js

Ouvrir navigateur : http://localhost:8099/
> C'est bien ok


## Coté git, avant tout chose, activé clés ssh et connecter au github

src : https://www.cnblogs.com/zhangzhihui/p/19070736

$ ssh-keygen
Generating public/private ed25519 key pair.
Enter file in which to save the key (/c/Users/g.malinge/.ssh/id_ed25519):
Enter passphrase (empty for no passphrase):
Enter same passphrase again:
Your identification has been saved in /c/Users/g.malinge/.ssh/id_ed25519
Your public key has been saved in /c/Users/g.malinge/.ssh/id_ed25519.pub
....

    git init
    git add *
    git commit -m "First commit projet nodejs simple"
    git branch -M main
    git remote add origin git@github.com:guyemInc/webapp_nodejs_start.git
    git push -u origin main


--
-- Cote dockerHub c'est un peu compliqué... et au final j'arrive à :
--	How it works

Instead of running builds on GitHub runners, your builds execute on Docker's cloud infrastructure while maintaining the same familiar actions and configuration.

Ou aussi : Integrate with GitLab CI/CD
Integrating with GitLab CI/CD allows you to speed up your CI builds with a shared cache.
How it works

Instead of running builds on GitLab runners, your build scripts execute on Docker's cloud infrastructure.

--
-- Retour sur github, dans la zone Actions
--	https://github.com/guyemInc/webapp_nodejs_start/actions/new?category=none&query=docker
-- Chercher Docker image : Build a Docker image to deploy, run, or push to a registry. 
--puis Configure
--puis commit en laissant tout par defaut dans le docker-image.yml

... bon, le fichier généré est un exemple qui ne prend pas du tout en compte le contexte du code
... suppression depuis Github de toutes les actions en erreur (suite aux commits)
...	https://github.com/guyemInc/webapp_nodejs_start/actions


--
-- Installation de https://windsurf.com/
--	Dans : C:\Users\g.malinge\AppData\Local\Programs\Windsurf
--	Voir aussi : https://docs.windsurf.com/windsurf/getting-started

-- Objectif : lui demander de créer le fichier dockerfile qui va bien sur ce projet

comprend tu de quel type d'application il s'agit dans ce répertoire ?
WS : 	C'est un projet de démarrage minimaliste pour une application web Node.js/Express.
sais-tu créer un dockerfile qui puisse exécuter ce code en local sur windows ?
WS : J'ai créé un Dockerfile et un .dockerignore pour votre application.
	Le Dockerfile utilise Node.js 18 Alpine, installe les dépendances, expose le port 8099 et lance votre application.
sais tu ou positionner ce fichier pour qu'il soit compris par les actions github ?
WS : Le Dockerfile est déjà correctement positionné à la racine du projet. GitHub Actions le trouvera automatiquement.

	Configuration actuelle :

	Le Dockerfile est à la racine : c:\Informatique_tmp\Perso\Projets\webapp_nodejs_start\Dockerfile
	Le workflow GitHub existe déjà : .github/workflows/docker-image.yml
	Le workflow utilise docker build . --file Dockerfile qui cherche le Dockerfile à la racine
	Le workflow GitHub Actions va :

	Se déclencher sur push/PR vers la branche main
	Utiliser windows-latest comme runner
	Construire l'image Docker avec votre Dockerfile
	Aucun déplacement nécessaire - le fichier est au bon endroit. GitHub Actions le détectera automatiquement lors de l'exécution du workflow

git commit et git push pour voir résultat côté github
> nok

Question s'il sait interpréter : Step 1/7 : FROM node:18-alpine
	18-alpine: Pulling from library/node
	no matching manifest for windows/amd64 10.0.26100 in the manifest list entries
	Error: Process completed with exit code 1.

WS : 	Je vais modifier le Dockerfile pour utiliser une image Windows compatible.
	Le Dockerfile est maintenant configuré pour Windows avec :

		Base image : Windows Server Core 2019
		Installation Node.js : Télécharge et installe Node.js 18.19.0 via PowerShell
		Compatibilité : Fonctionnera avec windows-latest dans GitHub Actions
	Le workflow GitHub devrait maintenant construire correctement l'image Docker sur Windows.

Au final, il sur Windows Pro / Family il faut passer via Docker Desktop qui lui déclenche WSL 2 pour Windows.
	....

Et nouvelle erreur dans Github... évidemment : Il doit y avoir d'autres choses à modifier pour que cela fonctionne. Voici le 		message github : Step 1/7 : FROM node:18-alpine
		18-alpine: Pulling from library/node
		no matching manifest for windows/amd64 10.0.26100 in the manifest list entries
		Error: Process completed with exit code 1.
WS : Le workflow utilise encore windows-latest au lieu de ubuntu-latest. Je vais vérifier et corriger.

Et bien j'en apprend des choses, l'image doit soit être publiée sur DockerHub, soit sauvegardée téléchargeable 30j depuis github.
> Choix : sauvegarder l'image en tant qu'artifact téléchargeable
WS : Pour utiliser l'image :

	bash
	docker run -p 8099:8099 my-image-name:timestamp
	L'artifact sera disponible pendant 30 jours par défaut.

Tjrs pas d'artifact... Ok je vois bien Artifact mais il est vide. L'alternative proposée règlera t elle le sujet ?
WS : Oui, l'alternative devrait régler le problème. L'artifact était probablement vide à cause du timestamp dynamique qui créait un nom d'image variable.

25/01/2026 - Déploiement sur poste perso de ce test
	gravo@LAPTOP-CCEPA44H MINGW64 /d/data_guillaume/Projets/20260126 Sample Web dockerfile
	$ git clone https://github.com/guyemInc/webapp_nodejs_start.git

	Depuis Winsurl, positionnement sur le répertoire : 
		D:\data_guillaume\Projets\20260126 Sample Web dockerfile\webapp_nodejs_start
	
	Il comprend qu'il s'agit qu'il s'agit d'un projet nodejs et sait le démarrer
		Ok en localhost 8080

	Ensuite, à ma demande, il trouve bien un dockerfile permettant de lancer la même application
		Windsurf me propose des commandes que je ne parviens pas à lancer
	Lui y parvient en mettant au préalable à jour le port d'exécution en 8099

	Je place ici mes notes pendant que WindSurf met à jour le README etc avec les dernière mises à jour
		Ps, le http://localhost:8099/ via dockerfile fonctionne parfaitement suite à l'exécution du dockerfile
	
	Pour mémoire : 
	…\webapp_nodejs_start > docker --version
		Docker version 29.1.3, build f52814d
	#WS :  construire l'image Docker pour votre application.
	…\webapp_nodejs_start > docker build -t webapp-nodejs .

	#WS :  L'image Docker a été construite avec succès. 
	#WS :  Je vais maintenant lancer un conteneur à partir de cette image.
	…\webapp_nodejs_start > docker run -p 8099:8099 -d --name webapp-container webapp-nodejs

	#WS : Vous pouvez accéder à l'application en ouvrant votre navigateur à l'adresse : http://localhost:8099

	#WS : Pour gérer le conteneur, voici quelques commandes utiles :
		# Voir les conteneurs en cours d'exécution
		docker ps

		# Arrêter le conteneur
		docker stop webapp-container

		# Redémarrer le conteneur
		docker start webapp-container

		# Supprimer le conteneur (après l'avoir arrêté)
		docker rm webapp-container

		# Voir les logs du conteneur
		docker logs webapp-container

	git add *
	git commit -m "Mise à jour du port 8080 vers 8099 et documentation"
	git push
	
25/01/2026 - Contrôle côté github :

	Dans https://github.com/guyemInc/webapp_nodejs_start/actions/runs
	On a bien un run en cours après git push et la présence d'un artifact ;
		https://github.com/guyemInc/webapp_nodejs_start/actions/runs/21336301995#artifacts

